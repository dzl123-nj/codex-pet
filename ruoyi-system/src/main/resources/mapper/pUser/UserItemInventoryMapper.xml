<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.player.mapper.UserItemInventoryMapper">
    
    <resultMap type="UserItemInventory" id="UserItemInventoryResult">
        <result property="id"    column="id"    />
        <result property="userId"    column="user_id"    />
        <result property="itemId"    column="item_id"    />
        <result property="quantity"    column="quantity"    />
        <result property="createTime"    column="create_time"    />
        <result property="updateTime"    column="update_time"    />
    </resultMap>

    <sql id="selectUserItemInventoryVo">
        select id, user_id, item_id, quantity, create_time, update_time from user_item_inventory
    </sql>

    <select id="selectUserItemInventoryList" parameterType="UserItemInventory" resultMap="UserItemInventoryResult">
        <include refid="selectUserItemInventoryVo"/>
        <where>  
            <if test="userId != null "> and user_id = #{userId}</if>
            <if test="itemId != null "> and item_id = #{itemId}</if>
        </where>
    </select>
    
    <select id="selectUserItemInventoryById" parameterType="Long" resultMap="UserItemInventoryResult">
        <include refid="selectUserItemInventoryVo"/>
        where id = #{id}
    </select>

    <insert id="insertUserItemInventory" parameterType="UserItemInventory" useGeneratedKeys="true" keyProperty="id">
        insert into user_item_inventory
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="userId != null">user_id,</if>
            <if test="itemId != null">item_id,</if>
            <if test="quantity != null">quantity,</if>
            <if test="createTime != null">create_time,</if>
            <if test="updateTime != null">update_time,</if>
         </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="userId != null">#{userId},</if>
            <if test="itemId != null">#{itemId},</if>
            <if test="quantity != null">#{quantity},</if>
            <if test="createTime != null">#{createTime},</if>
            <if test="updateTime != null">#{updateTime},</if>
         </trim>
    </insert>

    <update id="updateUserItemInventory" parameterType="UserItemInventory">
        update user_item_inventory
        <trim prefix="SET" suffixOverrides=",">
            <if test="userId != null">user_id = #{userId},</if>
            <if test="itemId != null">item_id = #{itemId},</if>
            <if test="quantity != null">quantity = #{quantity},</if>
            <if test="createTime != null">create_time = #{createTime},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
        </trim>
        where id = #{id}
    </update>

    <delete id="deleteUserItemInventoryById" parameterType="Long">
        delete from user_item_inventory where id = #{id}
    </delete>

    <delete id="deleteUserItemInventoryByIds" parameterType="String">
        delete from user_item_inventory where id in 
        <foreach item="id" collection="array" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>
</mapper>